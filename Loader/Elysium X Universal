local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()

local Window = WindUI:CreateWindow({
    Title = "Elysium X Rework",
    Icon = "biohazard",
    Author = "By Zenith Studios",
    Folder = "Elysium X Rework",
    Transparent = true,
    Theme = "Crimson",
    NewElements = true,
    Resizable = false,
    HideSearchBar = false,
    ScrollBarEnabled = true
})

Window:SetToggleKey(Enum.KeyCode.G)

Window:Tag({
    Title = "V0.2",
    Color = Color3.fromHex("#990000"),
    Radius = 13,
})

Window:EditOpenButton({
    Title = "Elysium X",
    Icon = "shield-cog-corner",
    CornerRadius = UDim.new(0,50),
    StrokeThickness = 1,
    Color = ColorSequence.new(
        Color3.fromHex("FF0000"),
        Color3.fromHex("000080")
    ),
    OnlyMobile = false,
    Enabled = true,
    Draggable = true,
})

local MainTab = Window:Tab({ Title = "Main", Icon = "star", Locked = false })
local PlayerTab = Window:Tab({ Title = "Player", Icon = "user", Locked = false })
local MiscTab = Window:Tab({ Title = "Misc", Icon = "folder", Locked = false })
local SettingsTab = Window:Tab({ Title = "Settings", Icon = "cog", Locked = false})

MainTab:Paragraph({
    Title = "Open or Minimize UI IN PC",
    Desc = "PRESS G in your keyboard to open the ui or minimize the ui in pc",
    Color = "Green",
    Locked = false,
})

MainTab:Space()

MainTab:Paragraph({
    Title = "Zenith Studios",
    Desc = "! Links ¡",
    Thumbnail = "rbxassetid://81706308103426",
    ThumbnailSize = 85,
    Locked = false,
    Buttons = {
        {
            Icon = "globe",
            Title = "Website",
            Callback = function()
                setclipboard("https://hs1hydrax7.github.io/Zenith-Website/")
                WindUI:Notify({
                    Title = "Website",
                    Content = "Link Copied",
                    Duration = 3,
                    Icon = "link-2",
                })
            end,
        },
        {
            Icon = "message-circle",
            Title = "Discord",
            Callback = function()
                setclipboard("https://discord.gg/D97k9TTCq3")
                WindUI:Notify({
                    Title = "Discord",
                    Content = "Invite Copied",
                    Duration = 3,
                    Icon = "link-2",
                })
            end,
        },
    }
})

MainTab:Section({
    Title = "Interface customization",
    Icon = "paintbrush",
})

MainTab:Dropdown({
    Title = "Theme",
    Values = { "Rose","Light","MonokaiPro","Plant","Midnight","CottonCandy","Dark","Indigo","Sky","Crimson","Amber","Violet","Red" },
    Value = "Crimson",
    Callback = function(option)
        WindUI:SetTheme(option)
    end
})

MainTab:Toggle({
    Title = "Transparency",
    Icon = "palette",
    Value = true,
    Callback = function(state)
        Window:ToggleTransparency(state)
    end
})

MainTab:Section({
    Title = "User Profile",
    Icon = "shield-user",
})

MainTab:Toggle({
    Title = "Profile Icon",
    Desc = "Your profile on your left down",
    Icon = "user",
    Type = "Toggle",
    Value = false,
    Callback = function(state)
        local stateinfo = state
        if stateinfo then
            Window.User:Enable()
        else
            Window.User:Disable()
        end
    end
})

MainTab:Toggle({
    Title = "Set Profile Anonymous",
    Desc = "Your profile on your left down",
    Icon = "hat-glasses",
    Type = "Toggle",
    Value = false,
    Callback = function(state)
        local stateinfo = state
        if stateinfo then
            Window.User:SetAnonymous(true)
        else
            Window.User:SetAnonymous(false)
        end
    end
})

PlayerTab:Section({
    Title = "Player attributes",
    Icon = "user",
})

PlayerTab:Toggle({
    Title = "CFrame Speed",
    Desc = "",
    Value = false,
    Callback = function(state)

        local player = game:GetService("Players").LocalPlayer
        local RunService = game:GetService("RunService")

        _G.cframeSpeedEnabled = state
        _G.cframeSpeedValue = _G.cframeSpeedValue or 5

        local function getCharacter()
            for _,v in ipairs(workspace:GetDescendants()) do
                if v:IsA("Model") and v.Name == player.Name then
                    return v
                end
            end
        end

        if state then
            _G.cframeConn = RunService.RenderStepped:Connect(function(dt)
                if not _G.cframeSpeedEnabled then return end

                local char = getCharacter()
                if not char then return end

                local root = char:FindFirstChild("HumanoidRootPart")
                local hum = char:FindFirstChildOfClass("Humanoid")

                if root and hum and hum.MoveDirection.Magnitude > 0 then
                    root.CFrame = root.CFrame + (hum.MoveDirection * _G.cframeSpeedValue * dt)
                end
            end)
        else
            if _G.cframeConn then
                _G.cframeConn:Disconnect()
                _G.cframeConn = nil
            end
        end

    end
})

PlayerTab:Slider({
    Title = "CFrame Speed Value",
    Desc = "",
    Step = 1,
    Value = { Min = 0, Max = 100, Default = 5 },
    Callback = function(value)
        _G.cframeSpeedValue = value
    end
})

PlayerTab:Toggle({
    Title = "WalkSpeed",
    Desc = "",
    Value = false,
    Callback = function(state)

        local player = game:GetService("Players").LocalPlayer

        _G.wsEnabled = state
        _G.wsValue = _G.wsValue or 16

        local function apply()
            for _,v in ipairs(workspace:GetDescendants()) do
                if v:IsA("Model") and v.Name == player.Name then
                    local hum = v:FindFirstChildOfClass("Humanoid")
                    if hum then
                        hum.WalkSpeed = state and _G.wsValue or 16
                    end
                end
            end
        end

        if state then
            apply()
            _G.wsConn = player.CharacterAdded:Connect(function()
                task.wait(0.3)
                apply()
            end)
        else
            apply()
            if _G.wsConn then
                _G.wsConn:Disconnect()
                _G.wsConn = nil
            end
        end

    end
})

PlayerTab:Slider({
    Title = "WalkSpeed Value",
    Desc = "",
    Step = 1,
    Value = { Min = 0, Max = 300, Default = 16 },
    Callback = function(value)

        _G.wsValue = value

        if _G.wsEnabled then
            for _,v in ipairs(workspace:GetDescendants()) do
                if v:IsA("Model") and v.Name == game.Players.LocalPlayer.Name then
                    local hum = v:FindFirstChildOfClass("Humanoid")
                    if hum then
                        hum.WalkSpeed = value
                    end
                end
            end
        end

    end
})

PlayerTab:Toggle({
    Title = "JumpPower",
    Desc = "",
    Value = false,
    Callback = function(state)

        local player = game:GetService("Players").LocalPlayer

        _G.jpEnabled = state
        _G.jpValue = _G.jpValue or 50

        local function apply()
            for _,v in ipairs(workspace:GetDescendants()) do
                if v:IsA("Model") and v.Name == player.Name then
                    local hum = v:FindFirstChildOfClass("Humanoid")
                    if hum then
                        hum.UseJumpPower = true
                        hum.JumpPower = state and _G.jpValue or 50
                    end
                end
            end
        end

        if state then
            apply()
            _G.jpConn = player.CharacterAdded:Connect(function()
                task.wait(0.3)
                apply()
            end)
        else
            apply()
            if _G.jpConn then
                _G.jpConn:Disconnect()
                _G.jpConn = nil
            end
        end

    end
})

PlayerTab:Slider({
    Title = "JumpPower Value",
    Desc = "",
    Step = 1,
    Value = { Min = 0, Max = 300, Default = 50 },
    Callback = function(value)

        _G.jpValue = value

        if _G.jpEnabled then
            for _,v in ipairs(workspace:GetDescendants()) do
                if v:IsA("Model") and v.Name == game.Players.LocalPlayer.Name then
                    local hum = v:FindFirstChildOfClass("Humanoid")
                    if hum then
                        hum.JumpPower = value
                    end
                end
            end
        end

    end
})

PlayerTab:Toggle({
    Title = "Gravity",
    Desc = "",
    Value = false,
    Callback = function(state)

        _G.gravEnabled = state
        _G.gravValue = _G.gravValue or 196.2

        if state then
            workspace.Gravity = _G.gravValue
        else
            workspace.Gravity = 196.2
        end

    end
})

PlayerTab:Slider({
    Title = "Gravity Value",
    Desc = "",
    Step = 1,
    Value = { Min = 0, Max = 300, Default = 196 },
    Callback = function(value)

        _G.gravValue = value

        if _G.gravEnabled then
            workspace.Gravity = value
        end

    end
})

MiscTab:Section({
    Title = "External Scripts",
    Icon = "folder-open",
})

MiscTab:Button({
    Title = "Infinite Yield",
    Icon = "file",
    Locked = false,
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source"))()
    end
})

MiscTab:Button({
    Title = "Dex Explorer",
    Icon = "file",
    Locked = false,
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/infyiff/backup/main/dex.lua"))()
    end
})

MiscTab:Button({
    Title = "Dex Plus",
    Icon = "file",
    Locked = false,
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/ltseverydayyou/uuuuuuu/refs/heads/main/DexPlusBackup.luau"))()
    end
})

MiscTab:Section({
    Title = "Avatar Copier",
    Icon = "copy",
})

MiscTab:Input({
    Title = "Username",
    Desc = "",
    Placeholder = "username",
    Callback = function(text)
        _G._avatarUser = text
    end
})

MiscTab:Button({
    Title = "Copy Avatar",
    Callback = function()

        local Players = game:GetService("Players")
        local LocalPlayer = Players.LocalPlayer

        if not _G._avatarUser or _G._avatarUser == "" then return end

        local function apply_avatar(username)
            task.spawn(function()
                local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
                local hum = char:WaitForChild("Humanoid", 10)
                if not hum then return end

                if not _G._originalDesc then
                    _G._originalDesc = hum:GetAppliedDescription()
                end

                local targetPlayer = nil
                for _, p in ipairs(Players:GetPlayers()) do
                    if p.Name:lower() == username:lower() then
                        targetPlayer = p
                        break
                    end
                end

                local desc
                if targetPlayer and targetPlayer.Character then
                    local targetHum = targetPlayer.Character:FindFirstChildOfClass("Humanoid")
                    if targetHum then
                        desc = targetHum:GetAppliedDescription()
                    end
                end

                if not desc then
                    local ok, userId = pcall(Players.GetUserIdFromNameAsync, Players, username)
                    if not ok then return end
                    local ok2, d = pcall(Players.GetHumanoidDescriptionFromUserId, Players, userId)
                    if not ok2 then return end
                    desc = d
                end

                for _, c in ipairs(char:GetChildren()) do
                    if c:IsA("Accessory") or c:IsA("Hat") or c:IsA("BodyColors") or
                       c:IsA("CharacterMesh") or c:IsA("Shirt") or c:IsA("Pants") or
                       c:IsA("ShirtGraphic") then
                        c:Destroy()
                    end
                end

                pcall(function()
                    if hum.ApplyDescriptionClientServer then
                        hum:ApplyDescriptionClientServer(desc)
                    else
                        hum:ApplyDescription(desc)
                    end
                end)

                local bc = char:FindFirstChildOfClass("BodyColors") or Instance.new("BodyColors")
                bc.Parent = char
                bc.HeadColor3 = desc.HeadColor
                bc.TorsoColor3 = desc.TorsoColor
                bc.LeftArmColor3 = desc.LeftArmColor
                bc.RightArmColor3 = desc.RightArmColor
                bc.LeftLegColor3 = desc.LeftLegColor
                bc.RightLegColor3 = desc.RightLegColor
            end)
        end

        _G._lastAvatarUser = _G._avatarUser
        apply_avatar(_G._avatarUser)

        if _G._avatarRespawnConn then
            _G._avatarRespawnConn:Disconnect()
        end

        _G._avatarRespawnConn = Players.LocalPlayer.CharacterAdded:Connect(function()
            task.wait(0.65)
            if _G._lastAvatarUser then
                apply_avatar(_G._lastAvatarUser)
            end
        end)

    end
})

MiscTab:Button({
    Title = "Refresh Avatar",
    Callback = function()

        local Players = game:GetService("Players")
        local LocalPlayer = Players.LocalPlayer

        if not _G._originalDesc then return end

        local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
        local hum = char:FindFirstChildOfClass("Humanoid")
        if not hum then return end

        for _, c in ipairs(char:GetChildren()) do
            if c:IsA("Accessory") or c:IsA("Hat") or c:IsA("BodyColors") or
               c:IsA("CharacterMesh") or c:IsA("Shirt") or c:IsA("Pants") or
               c:IsA("ShirtGraphic") then
                c:Destroy()
            end
        end

        pcall(function()
            if hum.ApplyDescriptionClientServer then
                hum:ApplyDescriptionClientServer(_G._originalDesc)
            else
                hum:ApplyDescription(_G._originalDesc)
            end
        end)

        if _G._avatarRespawnConn then
            _G._avatarRespawnConn:Disconnect()
            _G._avatarRespawnConn = nil
        end

    end
})

SettingsTab:Section({
    Title = "General Settings",
    Icon = "cog",
})
 
SettingsTab:Paragraph({
    Title = "WARN",
    Desc = "Under Construction",
    Image = "hammer",
    ImageSize = 24,
})

SettingsTab:Section({
    Title = "Minor Settings",
    Icon = "cog",
})

SettingsTab:Button({
    Title = "Copy JobId",
    Desc = "",
    Locked = false,
    Callback = function()

        if setclipboard then
            setclipboard(game.PlaceId)
        end

    end
})

SettingsTab:Button({
    Title = "ServerHop",
    Desc = "",
    Locked = false,
    Callback = function()

        local TeleportService = game:GetService("TeleportService")
        local Players = game:GetService("Players")

        TeleportService:Teleport(game.PlaceId, Players.LocalPlayer)

    end
})

SettingsTab:Button({
    Title = "Rejoin",
    Desc = "",
    Locked = false,
    Callback = function()

        local TeleportService = game:GetService("TeleportService")
        local Players = game:GetService("Players")

        TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, Players.LocalPlayer)

    end
})
